# ~~Фаза 8: Уведомления~~ ❌ ОТМЕНЕНА

## Статус: ❌ **ОТМЕНЕНА** - Pull-only стратегия

---

## Почему отменена

Система фильтров работает **только по pull-стратегии** (запросы по требованию).

Автоматические уведомления, push-обновления и фоновые процессы **не реализуются**.

---

## ✅ Что есть вместо этого

### Pull-стратегия (явные запросы)

Клиент **сам запрашивает** данные когда нужно:

```typescript
// Ручное обновление
GET /adminizer/filters/:id/execute

// Получить количество
GET /adminizer/filters/:id/count
```

### Polling на стороне клиента (опционально)

Если нужна периодическая проверка, клиент может реализовать:

```typescript
// Пример: React hook для polling
const useFilterPolling = (filterId: string, intervalMs: number = 30000) => {
  const [data, setData] = useState(null);
  const [count, setCount] = useState(0);
  
  useEffect(() => {
    const fetchData = async () => {
      const response = await fetch(`/adminizer/filters/${filterId}/execute`);
      const result = await response.json();
      setData(result.data);
      setCount(result.total);
    };
    
    fetchData(); // Первая загрузка
    
    const interval = setInterval(fetchData, intervalMs);
    return () => clearInterval(interval);
  }, [filterId, intervalMs]);
  
  return { data, count };
};

// Использование
function MyFilterView() {
  const { data, count } = useFilterPolling('filter-id-123', 60000); // Каждую минуту
  
  return (
    <div>
      <h2>Records: {count}</h2>
      {/* ... */}
    </div>
  );
}
```

### Кнопка "Refresh" в UI

```tsx
function FilterResults({ filterId }) {
  const [isLoading, setIsLoading] = useState(false);
  
  const handleRefresh = async () => {
    setIsLoading(true);
    await refetchFilterData(filterId);
    setIsLoading(false);
  };
  
  return (
    <div>
      <button onClick={handleRefresh} disabled={isLoading}>
        {isLoading ? 'Loading...' : 'Refresh'}
      </button>
      {/* ... */}
    </div>
  );
}
```

---

## 🎯 Преимущества pull-стратегии

✅ **Простая архитектура** - нет фоновых процессов  
✅ **Предсказуемая нагрузка** - запросы только когда нужно  
✅ **Меньше кода** - не нужны детекторы изменений, планировщики, очереди  
✅ **Меньше точек отказа** - нет cron задач, webhook delivery, email queue  
✅ **Клиент контролирует** частоту обновления  
✅ **Проще отладка** - синхронные запросы вместо асинхронных событий  
✅ **Без состояния** - не нужно хранить снимки данных для сравнения  

---

## 📚 Связанные разделы

- [Фаза 3: CRUD фильтров](03-filter-crud.md) - основные операции с фильтрами
- [Фаза 7: Public API](07-public-api.md) - публичный доступ к фильтрам по ключу
- [PLAN.md](../PLAN.md) - общий план с пометкой о pull-стратегии

---

## ⚠️ Важно

Эта фаза **не будет реализована** ни сейчас, ни в будущем. Если потребуется функционал уведомлений, он должен быть реализован на уровне **клиентского приложения** через polling или внешние системы мониторинга.

