# План реализации Plugin System для Adminizer

**📂 Детальные планы по фазам:** phases/

> ⚠️ ВАЖНО: Весь код — ПСЕВДОКОД для понимания концепций.
> НЕ копируйте буквально! Адаптируйте под реальную архитектуру.

> ⚠️ КРИТИЧНО: Плагины получают доступ к инстансу Adminizer. Необходимо обеспечить
> контроль за тем, что плагины не могут нарушить целостность ядра. Плагины НЕ должны
> напрямую мутировать внутренние структуры — только через предоставленный API (PluginContext).

> ⚠️ КРИТИЧНО: Порядок инициализации плагинов важен — плагины могут зависеть друг от друга.
> Система должна поддерживать объявление зависимостей и topological sort при загрузке.

---

## 🗂️ Структура документации
- **Фаза 1: Plugin Core** → phases/01-plugin-core.md
- **Фаза 2: Plugin Lifecycle Hooks** → phases/02-plugin-lifecycle-hooks.md
- **Фаза 3: Plugin Config Integration** → phases/03-plugin-config-integration.md
- **Фаза 4: Plugin Routes & Middleware** → phases/04-plugin-routes-middleware.md
- **Фаза 5: Plugin Models & Data** → phases/05-plugin-models-data.md
- **Фаза 6: Plugin UI (Navigation, Menu, Widgets)** → phases/06-plugin-ui.md
- **Фаза 7: Plugin Frontend Components** → phases/07-plugin-frontend.md
- **Фаза 8: Plugin Controls** → phases/08-plugin-controls.md
- **Фаза 9: Plugin Discovery & Loading** → phases/09-plugin-discovery-loading.md
- **Фаза 10: Fixture Plugin (Demo)** → phases/10-fixture-plugin.md
- **Фаза 11: Тестирование** → phases/11-testing.md

---

## Обзор

Plugin System — механизм расширения Adminizer через подключаемые плагины. Каждый плагин — это самостоятельный модуль, который может: регистрировать модели, роуты, middleware, виджеты, контролы, навигационные пункты, каталоги; модифицировать конфигурацию; реагировать на события жизненного цикла. Плагины могут быть включены/выключены, имеют изолированный контекст и объявляют зависимости.

### Ключевые возможности
1. Декларативное определение плагинов (класс или функция-фабрика)
2. Lifecycle hooks: `onRegister`, `onInit`, `onLoaded`, `onDestroy`
3. Модификация конфига через `configModifier`
4. Регистрация роутов, middleware, моделей, виджетов, контролов, каталогов, навигации
5. Управление зависимостями между плагинами (topological sort)
6. Включение/выключение плагинов через конфиг
7. Frontend: lazy-load React-компонентов плагина
8. Auto-discovery плагинов из директории `plugins/`

---

## Архитектура

### Диаграмма компонентов

```
┌──────────────────────────────────────────────────────────────────┐
│                        Host Application                          │
│  (Express mainApp → adminizer.getMiddleware())                   │
├──────────────────────────────────────────────────────────────────┤
│                         Adminizer Core                           │
│                                                                  │
│  ┌─────────────┐   ┌──────────────┐   ┌───────────────────────┐ │
│  │ PluginManager│──▶│PluginHandler │──▶│ AbstractPlugin        │ │
│  │             │   │(registry)    │   │  ├ onRegister()       │ │
│  │ .register() │   │              │   │  ├ onInit()           │ │
│  │ .initAll()  │   │ .add()       │   │  ├ onLoaded()         │ │
│  │ .loadAll()  │   │ .get()       │   │  ├ onDestroy()        │ │
│  │ .destroy()  │   │ .getAll()    │   │  └ manifest{}         │ │
│  └─────┬───────┘   └──────────────┘   └───────────────────────┘ │
│        │                                                         │
│        ▼                                                         │
│  ┌─────────────────────────────────────┐                         │
│  │          PluginContext               │                         │
│  │  (safe API surface for plugins)     │                         │
│  │                                     │                         │
│  │  .registerRoute()                   │                         │
│  │  .registerMiddleware()              │                         │
│  │  .registerModel()                   │                         │
│  │  .registerWidget()                  │                         │
│  │  .registerControl()                 │                         │
│  │  .registerCatalog()                 │                         │
│  │  .addNavItem()                      │                         │
│  │  .addMenuItem()                     │                         │
│  │  .modifyConfig()                    │                         │
│  │  .getConfig()                       │                         │
│  │  .emitter (scoped)                  │                         │
│  │  .logger (scoped)                   │                         │
│  └─────────────────────────────────────┘                         │
│                                                                  │
│  Existing handlers:                                              │
│  ModelHandler │ WidgetHandler │ ControlsHandler │ CatalogHandler │
│  PolicyManager│ MenuHelper    │ Router          │ etc.           │
├──────────────────────────────────────────────────────────────────┤
│                        Frontend (React/Inertia)                  │
│  module.tsx ← lazy loads plugin React components                 │
└──────────────────────────────────────────────────────────────────┘
```

### Интеграция с существующей архитектурой

```
src/
├── lib/
│   ├── Adminizer.ts                     # РАСШИРИТЬ: добавить pluginManager
│   ├── plugin/                          # НОВАЯ ПАПКА
│   │   ├── AbstractPlugin.ts            # НОВЫЙ: базовый класс плагина
│   │   ├── PluginManager.ts             # НОВЫЙ: менеджер плагинов
│   │   ├── PluginHandler.ts             # НОВЫЙ: реестр плагинов
│   │   ├── PluginContext.ts             # НОВЫЙ: контекст/API для плагинов
│   │   └── PluginManifest.ts            # НОВЫЙ: интерфейс манифеста
│   ├── model/ModelHandler.ts            # БЕЗ ИЗМЕНЕНИЙ (используется через PluginContext)
│   ├── widgets/widgetHandler.ts         # БЕЗ ИЗМЕНЕНИЙ
│   ├── controls/ControlsHandler.ts      # БЕЗ ИЗМЕНЕНИЙ
│   └── catalog/CatalogHandler.ts        # БЕЗ ИЗМЕНЕНИЙ
├── system/
│   ├── bindPlugins.ts                   # НОВЫЙ: функция инициализации плагинов
│   └── Router.ts                        # РАСШИРИТЬ: поддержка роутов плагинов
├── interfaces/
│   ├── adminpanelConfig.ts              # РАСШИРИТЬ: секция plugins в конфиге
│   └── pluginTypes.ts                   # НОВЫЙ: типы для Plugin API
├── index.ts                             # РАСШИРИТЬ: экспорт Plugin API
└── assets/js/
    └── pages/module.tsx                 # БЕЗ ИЗМЕНЕНИЙ (уже поддерживает dynamic import)

plugins/                                 # НОВАЯ ПАПКА (пример плагинов)
fixture/
└── plugins/                             # НОВАЯ ПАПКА (fixture плагины)
```

---

## Фаза 1: Plugin Core (базовые абстракции)

### Ключевые компоненты:
- `AbstractPlugin` — базовый класс/интерфейс плагина
- `PluginManifest` — декларативное описание плагина (id, name, version, dependencies)
- `PluginHandler` — реестр плагинов (add/get/remove)
- `PluginContext` — безопасный API для плагинов

📖 Детали: phases/01-plugin-core.md

---

## Фаза 2: Plugin Lifecycle Hooks

### Ключевые компоненты:
- `PluginManager` — оркестратор жизненного цикла
- Hooks: `onRegister` → `onInit` → `onLoaded` → `onDestroy`
- Topological sort для порядка инициализации
- Интеграция с EventEmitter Adminizer

📖 Детали: phases/02-plugin-lifecycle-hooks.md

---

## Фаза 3: Plugin Config Integration

### Ключевые компоненты:
- Секция `plugins` в `AdminpanelConfig`
- `configModifier` — хук для модификации конфига
- Включение/выключение плагинов через конфиг

📖 Детали: phases/03-plugin-config-integration.md

---

## Фаза 4: Plugin Routes & Middleware

### Ключевые компоненты:
- `PluginContext.registerRoute()` — регистрация роутов
- `PluginContext.registerMiddleware()` — регистрация middleware
- Namespace-изоляция роутов плагинов
- Интеграция с PolicyManager

📖 Детали: phases/04-plugin-routes-middleware.md

---

## Фаза 5: Plugin Models & Data

### Ключевые компоненты:
- `PluginContext.registerModel()` — регистрация моделей
- Интеграция с существующим ModelHandler
- Поддержка миграций плагинов

📖 Детали: phases/05-plugin-models-data.md

---

## Фаза 6: Plugin UI (Navigation, Menu, Widgets)

### Ключевые компоненты:
- `PluginContext.registerWidget()` — виджеты
- `PluginContext.addNavItem()` — пункты навигации
- `PluginContext.addMenuItem()` — элементы меню
- `PluginContext.registerCatalog()` — каталоги

📖 Детали: phases/06-plugin-ui.md

---

## Фаза 7: Plugin Frontend Components

### Ключевые компоненты:
- Lazy-load React компонентов из плагинов
- Vite config для сборки плагинов
- Механизм CSS-isolation

📖 Детали: phases/07-plugin-frontend.md

---

## Фаза 8: Plugin Controls

### Ключевые компоненты:
- `PluginContext.registerControl()` — кастомные контролы
- Интеграция с ControlsHandler
- AbstractControls для плагинов

📖 Детали: phases/08-plugin-controls.md

---

## Фаза 9: Plugin Discovery & Loading

### Ключевые компоненты:
- Auto-discovery из `plugins/` директории
- Загрузка по конвенции (index.ts экспортирует плагин)
- npm-пакеты как плагины

📖 Детали: phases/09-plugin-discovery-loading.md

---

## Фаза 10: Fixture Plugin (Demo)

### Ключевые компоненты:
- Перенос текущей fixture-логики в плагин-пример
- Демонстрация всех возможностей Plugin API

📖 Детали: phases/10-fixture-plugin.md

---

## Фаза 11: Тестирование

### Ключевые компоненты:
- Unit тесты для PluginManager, PluginHandler, PluginContext
- Integration тесты для полного жизненного цикла
- Тест fixture plugin

📖 Детали: phases/11-testing.md

---

## Зависимости (npm)

Новые npm-пакеты НЕ требуются. Вся функциональность реализуется на базе существующего стека:
- EventEmitter (Node.js built-in)
- Express Router (уже есть)
- Vite (уже есть, для сборки frontend плагинов)
- TypeScript generics/interfaces

---

## Тестирование
📖 Полная стратегия: TESTING_STRATEGY.md

---

## Оценка сложности по фазам

| Фаза | SP  | Сложность | Описание |
|------|-----|-----------|----------|
| 1    | 8   | Высокая   | Базовые абстракции, архитектура API |
| 2    | 5   | Средняя   | Lifecycle + topological sort |
| 3    | 3   | Низкая    | Config interface + merge |
| 4    | 5   | Средняя   | Роуты + middleware + namespace |
| 5    | 5   | Средняя   | Модели + миграции |
| 6    | 5   | Средняя   | UI-интеграции |
| 7    | 5   | Средняя   | Frontend lazy-load + vite |
| 8    | 3   | Низкая    | Controls integration |
| 9    | 5   | Средняя   | Discovery + dynamic import |
| 10   | 3   | Низкая    | Demo plugin |
| 11   | 5   | Средняя   | Тестирование |
| **Итого** | **52** | | |
